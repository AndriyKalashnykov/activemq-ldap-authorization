# Committed source-of-truth defaults for operator-tunable values.
# Copy to `.env` (gitignored) to override locally; e2e/scripts source this
# file first, then `.env`. Demo credentials below are NOT secrets — they are
# {SHA} hashes of "admin" seeded into the LDIF fixtures.

# --- ActiveMQ broker ---
ACTIVEMQ_VER=5.19.6
ACTIVEMQ_CONSOLE_HOST=localhost
ACTIVEMQ_CONSOLE_PORT=8161
ACTIVEMQ_OPENWIRE_PORT=61616

# --- LDAP backends ---
LDAP_HOST=localhost
LDAP_PORT=389
LDAPS_PORT=636
APACHEDS_PORT=10389

# --- Demo credentials (NOT secrets) ---
LDAP_ADMIN_USER=admin
LDAP_ADMIN_PASSWORD=admin
DEMO_USER=user
DEMO_USER_PASSWORD=admin
LDAP_BASE_DN=dc=activemq,dc=apache,dc=org

# --- Container names (compose service/container names) ---
ACTIVEMQ_CONTAINER=activemq
OPENLDAP_CONTAINER=openldap
SAMBA_CONTAINER=samba-ad

# Self-built OpenLDAP image (Symas packages; openldap/Dockerfile). The compose
# files build it via `build:` and tag it with this name.
OPENLDAP_IMAGE=activemq-openldap:latest

# --- Samba AD domain controller (alternative backend) ---
# Image built by `make build-samba`. The container hostname becomes the
# Kerberos realm (samba-ad-setup.sh provisions with --realm=$(hostname)).
# SMB_ADMIN_PASSWORD must meet AD complexity (>=8 chars, mixed classes);
# it is a demo value, not a secret.
SAMBA_IMAGE=samba-ad
SAMBA_VER=latest
SAMBA_HOST=dc.devad.example.com
SMB_ADMIN_PASSWORD=P@ssw0rd123!

# --- e2e harness tunables ---
COMPOSE_FILE=5.19.6/docker-compose.yml
E2E_READY_TIMEOUT_SECONDS=120
E2E_POLL_INTERVAL_SECONDS=2
